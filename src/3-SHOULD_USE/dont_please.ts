
var e = ""; //=> let or const

const equ = 2 == 2  //=> "===" instead ! 
const inequ = 2 != 2  //=> "!==" instead ! 

let r = 3;
r += 3 //=> r = r + 3 instead !
r -= -3 //=> r = r - 3 instead !
r *= 3 //=> r = r - 3 instead !

r **= 2 //=>r = r**2 instead !
r++ // r= r+1 instead !
r-- // r= r-1 instead !





// class  ===> functions
